.post
  .created-at
    = l @post.created_at, format: :long
  h1.title
    = @post.title
  .categories
    - for category in @post.categories do
      = link_to(category.name, posts_path(category: category.name), class: 'label label-category')

  article.content
    = @post.content_html
